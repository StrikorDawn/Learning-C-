=====================================
            Fraction
-------------------------------------
    - _top: int
    - _bottom: int

-------------------------------------
    + Fraction()
    + Fraction(wholeNumber: int)
    + Fraction(top: int, bottom: int)

    + GetTop()
    + SetTop()
    + GetBottom()
    + SetBottom()
    
    + GetFractionString() : string
    + GetDecimalValue() : double

======================================